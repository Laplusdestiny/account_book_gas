# account_book_gas 開発原則

## プロジェクト概要
- Google Apps Script (GAS) 上で動く家計簿レポートアプリ。
- `code.js`（サーバーサイド）と `index.html`（フロントエンド一式、CSS/JS込み）の2ファイル構成。
- データソースは Google スプレッドシート（Google フォームからの入力を集計）。
- 利用者は開発者本人＋家族。保守（コード修正）は本人のみが行う。

## デプロイ・同期のルール
- clasp 等の自動同期は未設定。**このリポジトリのファイルを編集し、完了したらGASのWebエディタに手動でコピー＆ペーストして反映する**運用が前提。
- Claude はこのリポジトリのファイルしか編集できず、実際のGAS環境には反映されない。変更を加えたら**「GASエディタへの反映が必要」であることを毎回明示する**。
- GASエディタ側で見比べやすいよう、一度の変更で大量の差分を作らない。
- 将来 clasp を導入する場合は、`.clasp.json` / `appsscript.json` の構成方針をこのファイルに追記する。

## アーキテクチャ原則
- 「サーバー1ファイル(`code.js`) + クライアント1ファイル(`index.html`、CSS/JS込み)」という現状のシンプルな構成を維持する。GASプロジェクトのファイル数を増やすと管理が煩雑になるため、明確な理由がない限りファイル分割はしない。
- `doGet()` はエントリーポイントとして薄く保つ。ロジックを増やさない。
- 集計・整形ロジックはクライアント側（`index.html` 内のJS）に寄せ、サーバー側はスプレッドシートとのやり取りに専念させる現状の役割分担を踏襲する。
- 家族が使うツールという性質上、フレームワーク導入や過剰な抽象化はしない（バニラJS + Chart.jsを維持）。

## GAS特有の制約への対応
- 実行時間制限（6分/回）を意識する。スプレッドシートの行数増加で `getReportData()` が重くなる場合は `CacheService.getScriptCache()` の利用を検討する。
- `getValues()` ではなく `getDisplayValues()` を使う現状の方針を継続する。スプレッドシートの表示形式に起因する日付・数値の型崩れを防ぐため。数値計算が必要な列は明示的に `Number()` で変換する。
- 日付文字列→`Date`変換の失敗時フォールバック（現状の `'不明'` 処理）は必ず維持する。スプレッドシートの日付フォーマットが変わっても壊れないようにする。
- スプレッドシートの列名（ヘッダー）とコード内のキー名（`'費目'`, `'支払い方法'` など）は密結合している。**スプレッドシートのヘッダーを変更する場合は、`code.js`・`index.html` 両方のキー参照を必ず一緒に確認・修正する**。
- HtmlServiceのCSP制約上、外部リソースは信頼できるCDN（現状Chart.jsのjsdelivr）に限定し、むやみに増やさない。
- `google.script.run` の非同期性を前提に設計する。サーバー呼び出しは `withSuccessHandler` と `withFailureHandler` を必ずセットで使い、失敗時にローディング表示から抜けられなくならないようにする。

## UI/UX・モバイル対応の原則
- 家族は主にスマートフォンから閲覧する想定。新しいUI要素を追加する際は必ず `@media (max-width: 600px)` のスタイルも同時に更新する。
- 金額は必ず `toLocaleString()` で3桁区切り表示にし、`¥` プレフィックスで統一する。
- 収入/支出の判定ロジック（`isIncome` 相当の条件）はサマリー集計・テーブル描画・明細行スタイリングの複数箇所に分散している。判定条件を変える場合はこの全箇所を漏れなく直す。
- フィルタ（月・支払い方法・支払い者・利用者）に手を入れる場合、選択肢生成側（`initializeData`）と絞り込みロジック側（`updateReport`）の両方を対応させる。
- UIを大きく変更する場合は frontend-design スキル（apm経由で導入済み）を活用し、家族が迷わず使えるレイアウトを優先する。

## データの扱い（個人情報配慮）
- 家計簿データは個人情報。デプロイ時のアクセス権限（Webアプリの公開範囲）やスプレッドシートの共有範囲を不用意に広げない。
- Googleフォーム／スプレッドシートのURLなどをコードに書く場合、リポジトリを公開する可能性がないか意識する。

## テスト・検証の進め方
GASにはユニットテスト環境がないため、変更後は以下を手動確認する。
- GASエディタ上で構文・実行エラーが出ないか。
- 実際のWebアプリURLでPC・スマホ両方の画面幅を確認する。
- データ0件／1件／複数月をまたぐ場合に表示が崩れないか。
- フィルタの組み合わせを変えたとき、サマリー金額・グラフ・明細テーブルの数字が一致するか。
- 金額計算ロジックに関わる変更は、可能であれば小さいテスト用データで先に確認してから本番データに適用する。

## コーディングスタイル
- 変数名・オブジェクトキーはスプレッドシートの日本語ヘッダーとの対応を優先し、既存の日本語プロパティ名（`'費目'` など）をそのまま使う。無理に英語化しない。
- コメントは「なぜその実装にしたか」（例: `getDisplayValues` を使う理由）だけを書き、何をしているかの説明コメントは書かない。
- 2ファイル構成を崩してまでの過剰なモジュール分割はしない。

## Claude Codeとの協働方針
- ファイル分割・外部ライブラリ追加・clasp移行など構成に関わる変更を提案する前に、必ずユーザーに確認する。
- 変更後は「GASエディタへの反映が必要」であることを毎回明示する。
- 金額計算ロジックの変更は特に慎重に扱い、変更前後で集計結果がどう変わるかをユーザーに説明する。
